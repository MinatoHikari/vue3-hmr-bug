import { baseUrl, getRequest, postRequest } from '~/utils/http/http';
import type { NavDataOrigin } from '~/utils/router';

export const getExampleKey = '/api/user/ticket';

export const commonRequests = {};
